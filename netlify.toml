# Netlify build and deploy config for Offee OKR (Vite + React)
[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes so React Router works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
